import { SubtaskCompletedCountPipe } from './subtask-completed-count.pipe';

describe('SubtaskCompletedCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtaskCompletedCountPipe();
    expect(pipe).toBeTruthy();
  });
});
